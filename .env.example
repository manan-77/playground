# Finnhub API key (free tier) — enables real Earnings, Analyst Trends, and
# company fundamentals (market cap, industry, IPO, website) on the stock page.
# Get one at https://finnhub.io/register, then copy this file to `.env`:
#   cp .env.example .env
# and restart `npm run dev`.
VITE_FINNHUB_KEY=
