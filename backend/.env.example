# Azure AI Foundry — copy to backend/.env and fill in, then `az login`.
# Project connection string from your Azure AI Foundry project (Overview page).
PROJECT_CONNECTION_STRING=
# The deployed Foundry IQ agent/assistant id (e.g. asst_...).
FOUNDRY_RESEARCH_AGENT_ID=
